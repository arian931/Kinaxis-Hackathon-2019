var enemie = function(x,y,w,h,speed){
    this.x = x; 
    this.y = y;
    this.speed = speed   
    this.w = w;
    this.h = h;
    this.update = function(){
      this.x = this.x + this.speed 
      this.y = this.y + this.speed
      //image 
      ellipse = (this.x,this.y,this.w,this.h,this.speed)
}
};

var enemie = []; 
for(const i=0; i<10;i++){
    enemie[i] = new enemie(random(),random(),10,10,20,20)
};

var create = function() {
    for(const i=0;i<enemie;i++){
    dot[i].update
}
